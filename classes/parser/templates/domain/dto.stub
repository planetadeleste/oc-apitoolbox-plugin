<?php

namespace {{namespace}};

use {{modelNamespace}}\{{modelName}};
use PlanetaDelEste\ApiToolbox\Contracts\DtoDomainInterface;

/**
 * Data Transfer Object para {{modelName}}
 *
 * @implements DtoDomainInterface<{{modelName}}>
 */
class {{modelName}}Data implements DtoDomainInterface
{
    public function __construct(
{{properties}}
    ) {
    }

    public static function fromRequest(array $arData): self
    {
        return new self(
            id: $arData['id'] ?? null,
{{fromRequest}}
        );
    }

    public static function from($obModel): self
    {
        return new self(
            id: $obModel->id,
{{fromModel}}
        );
    }

    public function toArray(): array
    {
        return [
{{toArray}}
        ];
    }
}
